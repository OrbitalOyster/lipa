# lipa

Requirements:
* docker buildx
* storage folder for db
* .env file

To run:
```bash
docker compose build
docker compose up -d
```

To view logs:
```bash
docker compose logs
```

To stop:
```bash
docker compose down
```
